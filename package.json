{
  "private": true,
  "scripts": {
    "build-legacy": "spago-legacy bundle-app --to dist/tunebank.js",
    "test-legacy": "spago-legacy -x ./test.dhall test",
    "optimise": "npm run uglify --s dist/tunebank.js --output dist/tunebankmin.js --compress --mangle"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "purescript": "^0.15.15",
    "spago": "0.21.0"
  },
  "dependencies": {
    "uglify": "^0.1.5",
    "uglify-js": "^3.5.11",
    "xhr2": "^0.2.1",
    "xmlhttprequest": "^1.8.0"
  }
}
